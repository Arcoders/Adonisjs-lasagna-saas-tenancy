apiVersion: v1
kind: Service
metadata:
  name: {{ include "lasagna-app.fullname" . }}
  labels:
    {{- include "lasagna-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lasagna-app.selectorLabels" . | nindent 4 }}
