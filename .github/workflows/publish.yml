name: Publish Packages

# Release is tag-driven: push a `v*` tag (e.g. `v1.0.0`) to publish. This
# replaces the previous "publish on every push to master", which tried to
# publish on every merge and failed noisily once the version already existed.
# Each package is published only if its exact version isn't already on npm, so
# re-running a tag (or a partial failure) is safe and idempotent.
on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  publish:
    name: Build & Publish (core + satellites)
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          registry-url: 'https://registry.npmjs.org'

      # Repo .npmrc sets `ignore-scripts=true` for local-dev safety; CI needs
      # esbuild's postinstall to run so the platform binary lands on disk.
      - name: Install dependencies
        env:
          npm_config_ignore_scripts: 'false'
        run: npm install --legacy-peer-deps --include=optional

      - name: Typecheck
        run: npm run typecheck

      # Build core first, then the satellites (admin imports sso; all import the
      # core via the workspace symlink to its build).
      - name: Build all packages
        run: npm run build:all

      - name: Publish (idempotent, dependency order)
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          set -euo pipefail

          publish_pkg() {
            # $1 = workspace dir (use "." for the root/core package)
            local dir="$1"
            local name version tag
            name=$(node -p "require('./$dir/package.json').name")
            version=$(node -p "require('./$dir/package.json').version")
            # Prerelease versions (anything after a `-`) go under the `next`
            # dist-tag so they never override `latest`.
            if [[ "$version" == *-* ]]; then tag="next"; else tag="latest"; fi

            if npm view "$name@$version" version >/dev/null 2>&1; then
              echo "✓ $name@$version already published — skipping"
              return 0
            fi

            echo "→ publishing $name@$version (dist-tag: $tag)"
            if [[ "$dir" == "." ]]; then
              npm publish --access public --tag "$tag"
            else
              npm publish --workspace "$name" --access public --tag "$tag"
            fi
          }

          # Core first so satellites resolve a published peer.
          publish_pkg "."
          publish_pkg "packages/sso"
          publish_pkg "packages/billing"
          publish_pkg "packages/admin"
          publish_pkg "packages/backup"
