{
  "commands": [
    {
      "commandName": "tenant:ai:audit:verify",
      "description": "Verify the append-only AI audit hash chain and report the first tamper (checksum / gap / prev-link)",
      "help": "Suggested: run in a cron or a post-incident gate. `node ace tenant:ai:audit:verify --json || alert`",
      "namespace": "tenant",
      "aliases": [],
      "flags": [
        {
          "name": "tenant",
          "flagName": "tenant",
          "required": false,
          "type": "string",
          "description": "Verify a single tenant (uuid); omit to verify every tenant"
        },
        {
          "name": "json",
          "flagName": "json",
          "required": false,
          "type": "boolean",
          "description": "Emit a JSON result"
        }
      ],
      "args": [],
      "options": { "startApp": true },
      "filePath": "ai_audit_verify.js"
    },
    {
      "commandName": "tenant:ai:purge",
      "description": "Erase a tenant, principal, or document AI data (memory + response cache + embeddings) for GDPR",
      "help": "Scopes: --tenant <uuid> --force (all); --tenant <uuid> --principal <id> (one user); --tenant <uuid> --source <key> (one document). --dry-run previews and writes nothing.",
      "namespace": "tenant",
      "aliases": [],
      "flags": [
        {
          "name": "tenant",
          "flagName": "tenant",
          "required": false,
          "type": "string",
          "description": "Tenant id (uuid) to purge (required)"
        },
        {
          "name": "principal",
          "flagName": "principal",
          "required": false,
          "type": "string",
          "description": "Purge only this principal (per-user GDPR erasure: memory + their embeddings)"
        },
        {
          "name": "source",
          "flagName": "source",
          "required": false,
          "type": "string",
          "description": "Purge only this document source key"
        },
        {
          "name": "dryRun",
          "flagName": "dry-run",
          "required": false,
          "type": "boolean",
          "description": "Preview the counts and write nothing (no delete, no epoch bump)"
        },
        {
          "name": "force",
          "flagName": "force",
          "required": false,
          "type": "boolean",
          "alias": "y",
          "description": "Required for a full-tenant purge (skips the confirmation)"
        },
        {
          "name": "verifyChain",
          "flagName": "verify-chain",
          "required": false,
          "type": "boolean",
          "description": "Also re-walk the full AI audit chain and record the result (O(n))"
        },
        {
          "name": "actor",
          "flagName": "actor",
          "required": false,
          "type": "string",
          "description": "Operator identity recorded in the audit row (defaults to the OS user)"
        }
      ],
      "args": [],
      "options": { "startApp": true },
      "filePath": "tenant_ai_purge.js"
    },
    {
      "commandName": "tenant:ai:audit:export",
      "description": "Export the append-only AI audit chain (NDJSON | CSV) in (tenant_id, seq) order for external re-verification",
      "help": "NDJSON (default) streams into a SIEM; CSV escapes formula injection. `node ace tenant:ai:audit:export --tenant <uuid> --out chain.ndjson`",
      "namespace": "tenant",
      "aliases": [],
      "flags": [
        {
          "name": "tenant",
          "flagName": "tenant",
          "required": false,
          "type": "string",
          "description": "Export a single tenant (uuid); omit for every tenant"
        },
        {
          "name": "from",
          "flagName": "from",
          "required": false,
          "type": "string",
          "description": "ISO 8601 lower bound on occurred_at (inclusive)"
        },
        {
          "name": "to",
          "flagName": "to",
          "required": false,
          "type": "string",
          "description": "ISO 8601 upper bound on occurred_at (inclusive)"
        },
        {
          "name": "format",
          "flagName": "format",
          "required": false,
          "type": "string",
          "description": "Output format: ndjson | csv"
        },
        {
          "name": "out",
          "flagName": "out",
          "required": false,
          "type": "string",
          "description": "Write to this file instead of stdout (overwritten if it exists)"
        }
      ],
      "args": [],
      "options": { "startApp": true },
      "filePath": "ai_audit_export.js"
    },
    {
      "commandName": "tenant:ai:audit:archive",
      "description": "Archive a tenant AI audit segment: verify, export to WORM/SIEM in chain order, then advance a signed checkpoint (prunes nothing)",
      "help": "The default retention path prunes nothing. `node ace tenant:ai:audit:archive --tenant <uuid> --out worm.ndjson`",
      "namespace": "tenant",
      "aliases": [],
      "flags": [
        {
          "name": "tenant",
          "flagName": "tenant",
          "required": false,
          "type": "string",
          "description": "Tenant (uuid) to archive (required; a checkpoint is per-tenant)"
        },
        {
          "name": "from",
          "flagName": "from",
          "required": false,
          "type": "string",
          "description": "ISO 8601 lower bound on occurred_at (inclusive)"
        },
        {
          "name": "to",
          "flagName": "to",
          "required": false,
          "type": "string",
          "description": "ISO 8601 upper bound on occurred_at (inclusive)"
        },
        {
          "name": "format",
          "flagName": "format",
          "required": false,
          "type": "string",
          "description": "Export format: ndjson | csv"
        },
        {
          "name": "out",
          "flagName": "out",
          "required": false,
          "type": "string",
          "description": "The WORM/SIEM destination file (required)"
        }
      ],
      "args": [],
      "options": { "startApp": true },
      "filePath": "ai_audit_archive.js"
    }
  ]
}
