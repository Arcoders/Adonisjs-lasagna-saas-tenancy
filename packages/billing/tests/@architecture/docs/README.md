# @architecture/docs

Integrity specs (the *_documented guards) that fail when code drifts from its documentation.

Runs in the unit harness (no database). Placeholder until the first docs spec
lands here; the README keeps the slot visible and tracked.
