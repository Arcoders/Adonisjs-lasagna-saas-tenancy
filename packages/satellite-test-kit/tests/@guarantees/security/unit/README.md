# @guarantees/security/unit

Specs proving the **security** guarantee in the unit harness, which runs against source with tsx, no database.

Name new specs `security_<context>_<outcome>.spec.ts`. This directory is a
placeholder until the first security unit spec lands; the README keeps
the slot visible and tracked. Delete it once specs live here.
