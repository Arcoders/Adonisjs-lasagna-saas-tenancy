# @guarantees/behavior/integration

Specs proving the **behavior** guarantee in the integration harness, which boots the shared AdonisJS Ignitor and PostgreSQL.

Name new specs `behavior_<context>_<outcome>.spec.ts`. This directory is a
placeholder until the first behavior integration spec lands; the README keeps
the slot visible and tracked. Delete it once specs live here.
