/**
 * The SSO identity-provider contract version: the shape of the
 * {@link IdentityProviderContract} surface (`buildAuthUrl` / `handleCallback`).
 * Bump as a MAJOR for a backward-incompatible change. INDEPENDENT of
 * `lasagnaSatellite.satelliteApi` and the published version.
 */
export const SSO_CONTRACT_VERSION = 1
