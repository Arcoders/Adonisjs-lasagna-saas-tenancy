# @guarantees/behavior/unit

Specs proving the **behavior** guarantee in the unit harness, which runs against source with tsx, no database.

Name new specs `behavior_<context>_<outcome>.spec.ts`. This directory is a
placeholder until the first behavior unit spec lands; the README keeps
the slot visible and tracked. Delete it once specs live here.
