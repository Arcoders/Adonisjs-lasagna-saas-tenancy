// The crypto satellite testing surface: in-process doubles for the default run,
// mirroring the AI satellite's `/testing` subpath.
export { default as InMemoryWrappedDekStore } from './in_memory_wrapped_dek_store.js'
