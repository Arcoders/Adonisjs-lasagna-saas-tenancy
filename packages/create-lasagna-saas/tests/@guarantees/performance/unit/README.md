# @guarantees/performance/unit

Specs proving the **performance** guarantee in the unit harness, which runs against source with tsx, no database.

Name new specs `performance_<context>_<outcome>.spec.ts`. This directory is a
placeholder until the first performance unit spec lands; the README keeps
the slot visible and tracked. Delete it once specs live here.
