# @guarantees/security/integration

Specs proving the **security** guarantee in the integration harness, which boots the shared AdonisJS Ignitor and PostgreSQL.

Name new specs `security_<context>_<outcome>.spec.ts`. This directory is a
placeholder until the first security integration spec lands; the README keeps
the slot visible and tracked. Delete it once specs live here.
