# @guarantees/isolation/integration

Specs proving the **isolation** guarantee in the integration harness, which boots the shared AdonisJS Ignitor and PostgreSQL.

Name new specs `isolation_<context>_<outcome>.spec.ts`. This directory is a
placeholder until the first isolation integration spec lands; the README keeps
the slot visible and tracked. Delete it once specs live here.
