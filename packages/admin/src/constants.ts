/**
 * The admin-action contract version: the shape of the {@link AdminAction}
 * surface (its `execute(ctx, signal?)` signature). Bump as a MAJOR for a
 * backward-incompatible change. INDEPENDENT of `lasagnaSatellite.satelliteApi`
 * and the published version.
 */
export const ADMIN_CONTRACT_VERSION = 1
